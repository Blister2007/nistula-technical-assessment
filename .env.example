# OPTIONAL: This file is not needed for the assessment to run.
#
# The backend defaults to a live Cloudflare Worker I deployed
# specifically for this assessment, so it works out of the box.
# Just install requirements.txt and run python run.py.
#
# Only fill in these values if you want to deploy your own Worker
# (see the README section "Deploying your own Worker").

# WORKER_URL=https://your-worker.workers.dev
# PROXY_AUTH_TOKEN=your-random-string