# Copy this file to .env and fill in both values.
#
# Deploy your own Cloudflare Worker first (see the README Quick Start),
# then paste its URL and the auth token you set on it here.
#
# .env is gitignored so these never get committed.

WORKER_URL=https://your-worker.workers.dev
PROXY_AUTH_TOKEN=your-auth-token-here
